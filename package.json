{
  "name": "dynamic-page-generator",
  "version": "1.0.0",
  "description": "Dynamic AI-powered page generator",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "vercel-build": "cd backend && npm install && npm run build && node scripts/copy-dist.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
