{
  "name": "dynamic-page-generator",
  "version": "1.0.0",
  "description": "Dynamic AI-powered page generator",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "vercel-build": "npm install && cd backend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@vercel/kv": "^3.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}